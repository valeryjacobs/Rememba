<Page
    x:Class="Rememba.Windows.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Rememba.Windows.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   xmlns:conv="using:Rememba.Windows.Converters"
    mc:Ignorable="d"               
    DataContext="{Binding MainViewViewModel, Source={StaticResource MainViewModelLocator}}"
    >
    <Page.Resources>
        <conv:BindingInspector x:Key="inspector"></conv:BindingInspector>
        <conv:WebContentViewConverter x:Key="WebViewContentConverter" />
    </Page.Resources>
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"></ColumnDefinition>
            <ColumnDefinition Width="260"></ColumnDefinition>
            <ColumnDefinition Width="320"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1">
            <!--<Button Command="{Binding SelectParentNodeCommand}">Select</Button>
            <Button Command="{Binding SelectChildNodeCommand}">Select</Button>
            <Button Command="{Binding SelectSubChildNodeCommand}">Select</Button>
            <TextBlock Text="{Binding RootNode, Converter={StaticResource inspector}}"></TextBlock>-->
            <Button Command="{Binding GoUpTree}">Up</Button>
            <Button Command="{Binding Save}">Save</Button>
            <Button Command="{Binding AddContent}">Add content</Button>
            <Button Command="{Binding UpdateContent}">Update content</Button>
            <Button Command="{Binding DeleteContent}">Delete content</Button>
        </StackPanel>

        <ListView ItemsSource="{Binding ParentList}" SelectedValue="{Binding SelectedParent, Mode=TwoWay}" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5">
                        <TextBlock TextWrapping="Wrap" FontWeight="Bold"  Text="{Binding Title}"></TextBlock>
                        <TextBlock TextWrapping="Wrap"   Text="{Binding Description}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ListView Grid.Column="1" ItemsSource="{Binding ChildList}" x:Name="childListView"   SelectedValue="{Binding SelectedChild, Mode=TwoWay}"  >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5">
                        <TextBlock TextWrapping="Wrap" FontWeight="Bold"  Text="{Binding Title}"></TextBlock>
                        <TextBlock TextWrapping="Wrap" Text="{Binding Description}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ListView Grid.Column="2" ItemsSource="{Binding SubChildList}" SelectedValue="{Binding SelectedSubChild, Mode=TwoWay}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5">
                        <TextBlock TextWrapping="Wrap" FontWeight="Bold"   Text="{Binding Title}"></TextBlock>
                        <TextBlock  TextWrapping="Wrap"  Text="{Binding Description}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <WebView x:ConnectionId='1'                                          Grid.Column="3" Grid.RowSpan="2"  HorizontalAlignment="Stretch" DataContext="{Binding SelectedNodeContent}" Tag="{Binding Data, Converter={StaticResource WebViewContentConverter},ConverterParameter='contentView'}"  x:Name="contentView"></WebView>

    </Grid>
</Page>

