<Page
    x:Class="Rememba.Windows.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Rememba.Windows.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   xmlns:conv="using:Rememba.Windows.Converters"
    mc:Ignorable="d"               
    DataContext="{Binding MainViewViewModel, Source={StaticResource MainViewModelLocator}}"
    >
    <Page.Resources>
        <conv:BindingInspector x:Key="inspector"></conv:BindingInspector>
        <conv:WebContentViewConverter x:Key="WebViewContentConverter" />
        <conv:BoolToVisibilityConverter x:Key="boolToVisibilityConverter"/>
        <!--<BooleanToVisibilityConverter x:Key="boolToVisConverter"/>-->
        <Style TargetType="TextBox">
            <Setter  Property="Background" Value="Black"></Setter>
            <Setter  Property="Foreground" Value="White"></Setter>
            
        </Style>
    </Page.Resources>
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"></ColumnDefinition>
            <ColumnDefinition Width="260"></ColumnDefinition>
            <ColumnDefinition Width="320"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="4" Margin="0,0,0,10">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Height" Value="50"></Setter>
                    <Setter Property="Margin" Value="3"></Setter>
                    <Setter Property="FontSize" Value="9"></Setter>
                </Style>
            </StackPanel.Resources>
            <!--<TextBox Text="{Binding Path.MindMap.Name}"></TextBox>-->
            <!--<Button Command="{Binding SelectParentNodeCommand}">Select</Button>
            <Button Command="{Binding SelectChildNodeCommand}">Select</Button>
            <Button Command="{Binding SelectSubChildNodeCommand}">Select</Button>
            <TextBlock Text="{Binding RootNode, Converter={StaticResource inspector}}"></TextBlock>-->
            <ComboBox Width="240" Height="30" ItemsSource="{Binding Graphs}" SelectedItem="{Binding MindMap, Mode=TwoWay}" DisplayMemberPath="Name" >
            </ComboBox>
            <Button Command="{Binding LoadGraphCommand}">Load</Button>
            <Button Command="{Binding GoDo}">Do</Button>
     
            <Button Command="{Binding CreateGraphCommand}">Create Graph</Button>
            <Button Command="{Binding GoUpTree}">Up</Button>
            <Button Command="{Binding Save}">Save</Button>
            <Button Command="{Binding AddChildNodeCommand}">Add Child</Button>
            <Button Command="{Binding AddSiblingNodeCommand}">Add Sibling</Button>
            <Button Command="{Binding EditNodeCommand}">Edit node</Button>
            <Button Command="{Binding DeleteNodeCommand}">Delete node</Button>
            <Button Command="{Binding MoveUpOrder}">Order up</Button>
            <Button Command="{Binding MoveDownOrder}">Order down</Button>
            <Button Command="{Binding MoveUpTreeCommand}">Up tree</Button>

            <Button Command="{Binding AddContent}">Add content</Button>
            <Button x:ConnectionId='2' x:Name="switchModeButton" Command="{Binding EditContent}"                         >Edit</Button>
            <Button Command="{Binding UpdateContent}">Update content</Button>
            <Button Command="{Binding DeleteContent}" >Delete content</Button>
        </StackPanel>

        <ListView Grid.Row="1"  ItemsSource="{Binding ParentList}" SelectedValue="{Binding SelectedParent, Mode=TwoWay}" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        
                        <StackPanel Visibility="{Binding Edit, Converter={StaticResource boolToVisibilityConverter}}" Orientation="Vertical" Margin="5">
                            <TextBlock TextWrapping="Wrap" FontWeight="Bold"  Text="{Binding Title}"></TextBlock>
                            <TextBlock TextWrapping="Wrap"   Text="{Binding Description}"></TextBlock>
                        </StackPanel>
                        <StackPanel Visibility="{Binding Edit, Converter={StaticResource boolToVisibilityConverter},ConverterParameter=Flip}" Orientation="Vertical" Margin="5">
                            <TextBox Width="240" HorizontalAlignment="Stretch" TextWrapping="Wrap" FontWeight="Bold"  Text="{Binding Title,Mode=TwoWay}"></TextBox>
                            <TextBox Width="240" HorizontalAlignment="Stretch" TextWrapping="Wrap"  Text="{Binding Description,Mode=TwoWay}"></TextBox>
                           
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ListView Grid.Row="1"  Grid.Column="1" ItemsSource="{Binding ChildList}" x:Name="childListView"   SelectedValue="{Binding SelectedChild, Mode=TwoWay}"  >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>

                        <StackPanel Visibility="{Binding Edit, Converter={StaticResource boolToVisibilityConverter}}" Orientation="Vertical" Margin="5">
                            <TextBlock TextWrapping="Wrap" FontWeight="Bold"  Text="{Binding Title}"></TextBlock>
                            <TextBlock TextWrapping="Wrap"   Text="{Binding Description}"></TextBlock>
                        </StackPanel>
                        <StackPanel Visibility="{Binding Edit, Converter={StaticResource boolToVisibilityConverter},ConverterParameter=Flip}" Orientation="Vertical" Margin="5">
                            <TextBox Width="240" TextWrapping="Wrap" FontWeight="Bold"  Text="{Binding Title,Mode=TwoWay}"></TextBox>
                            <TextBox Width="240" TextWrapping="Wrap"  Text="{Binding Description,Mode=TwoWay}"></TextBox>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ListView Grid.Row="1"  Grid.Column="2" ItemsSource="{Binding SubChildList}" SelectedValue="{Binding SelectedSubChild, Mode=TwoWay}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>

                        <StackPanel Visibility="{Binding Edit, Converter={StaticResource boolToVisibilityConverter}}" Orientation="Vertical" Margin="5">
                            <TextBlock TextWrapping="Wrap" FontWeight="Bold"  Text="{Binding Title}"></TextBlock>
                            <TextBlock TextWrapping="Wrap"   Text="{Binding Description}"></TextBlock>
                        </StackPanel>
                        <StackPanel Visibility="{Binding Edit, Converter={StaticResource boolToVisibilityConverter},ConverterParameter=Flip}" Orientation="Vertical" Margin="5">
                            <TextBox Width="240" TextWrapping="Wrap" FontWeight="Bold"  Text="{Binding Title,Mode=TwoWay}"></TextBox>
                            <TextBox Width="240" TextWrapping="Wrap"  Text="{Binding Description,Mode=TwoWay}"></TextBox>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <WebView x:ConnectionId='1' Grid.Row="1"                                                                                                          Grid.Column="3"   HorizontalAlignment="Stretch" DataContext="{Binding SelectedNodeContent}" Tag="{Binding Data, Converter={StaticResource WebViewContentConverter},ConverterParameter='contentView'}"  x:Name="contentView"></WebView>

    </Grid>
</Page>

